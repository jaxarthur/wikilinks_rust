use svg::Document;
use svg::node::element::Circle;
use svg::node::element::Text;

pub fn render_graph(links: Vec<(String, String)>) -> String {
    let
    return String::new();
}

fn render_node(link: String) -> Circle {
    let circle = Circle::new();
    let text = Text::new();
    text.
}